{
  "name": "car-hono-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "build": "esbuild src/index.js --bundle --format=esm --target=esnext --outfile=dist/index.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "esbuild": "^0.24.0",
    "vitest": "2.1.8",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.5.0",
    "@hono/zod-openapi": "^0.18.3",
    "@neondatabase/serverless": "^0.10.4",
    "hono": "^4.6.19",
    "mongodb": "^6.12.0",
    "pg": "^8.13.1",
    "swagger-ui-express": "^5.0.1"
  }
}
